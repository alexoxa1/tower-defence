# Supabase project credentials for the browser app (Vite exposes VITE_* to the client).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Server-only values used by scripts/create-user.ts. Never expose the service role key to the client.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
NEO_USER_EMAIL=
NEO_USER_PASSWORD=
